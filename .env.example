# Copy this file to .env and adapt values for your local setup.

# Not needed for `uv run python pipeline/train_alpha_model.py` or `uv run pytest`
# (those already set the path via the script / pytest config).
PYTHONPATH=.

